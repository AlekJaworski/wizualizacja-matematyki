---
options:
  - "$x = 1$ (minimum)"
  - "$x = 1$ (maksimum)"
  - "$x = -1$ (minimum)"
  - "$x = 0$ (minimum)"
optionsEn:
  - "$x = 1$ (minimum)"
  - "$x = 1$ (maximum)"
  - "$x = -1$ (minimum)"
  - "$x = 0$ (minimum)"
correct: 0
hint: "$f'(x) = 2x - 2 = 0 \Rightarrow x = 1$. $f''(x) = 2 > 0$, więc to minimum."
hintEn: "$f'(x) = 2x - 2 = 0 \Rightarrow x = 1$. $f''(x) = 2 > 0$, so it's a minimum."
tests:
  deriv_apps: 1.0
  derivative: 0.5
  optimization_lp: 0.5
---
Funkcja $f(x) = x^2 - 2x + 3$ ma ekstremum w:

<!-- en -->
The function $f(x) = x^2 - 2x + 3$ has an extremum at: