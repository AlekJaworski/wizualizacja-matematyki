---
options:
  - "$(1, 0)$"
  - "$(0, 1)$"
  - "$(0, 0)$"
  - "$(a, 1)$"
optionsEn:
  - "$(1, 0)$"
  - "$(0, 1)$"
  - "$(0, 0)$"
  - "$(a, 1)$"
correct: 0
hint: "Dla każdej podstawy $a$: $\log_a 1 = 0$, więc wykres przechodzi przez $(1, 0)$."
hintEn: "For every base $a$: $\log_a 1 = 0$, so the graph passes through $(1, 0)$."
tests:
  fn_log: 1.0
  log_exp: 0.5
---
Wykres funkcji $f(x) = \log_a x$ zawsze przechodzi przez punkt:

<!-- en -->
The graph of the function $f(x) = \log_a x$ always passes through the point: