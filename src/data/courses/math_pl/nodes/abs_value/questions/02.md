---
options:
  - "$|x| = -x$ dla $x < 0$"
  - "$|x| = x$ dla każdego $x$"
  - "$|x| = -x$ dla $x > 0$"
  - "$|x|$ może być ujemna"
optionsEn:
  - "$|x| = -x$ for $x < 0$"
  - "$|x| = x$ for every $x$"
  - "$|x| = -x$ for $x > 0$"
  - "$|x|$ can be negative"
correct: 0
hint: "Wartość bezwzględna to odległość od zera — zawsze nieujemna. Dla $x < 0$: $|x| = -x > 0$."
hintEn: "Absolute value is the distance from zero — always non-negative. For $x < 0$: $|x| = -x > 0$."
tests:
  abs_value: 1.0
  linear_eq: 0.5
---
Które zdanie o wartości bezwzględnej jest prawdziwe?

<!-- en -->
Which statement about absolute value is true?