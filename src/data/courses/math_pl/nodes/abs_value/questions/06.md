---
source: cke_2025_rozszerzona_zad5
options:
  - "$x\in(-\infty,-10)\cup\left(-\tfrac{2}{3},+\infty\right)$"
  - "$x\in\left(-10,-\tfrac{2}{3}\right)$"
  - "$x\in(-\infty,+\infty)$"
  - "brak rozwiązań"
optionsEn:
  - "$x\in(-\infty,-10)\cup\left(-\tfrac{2}{3},+\infty\right)$"
  - "$x\in\left(-10,-\tfrac{2}{3}\right)$"
  - "$x\in(-\infty,+\infty)$"
  - "no solutions"
correct: 0
hints:
  - "① Nierówność $|x-2|-2|x+3|<-2$ ma dwa \"węzły\": $x=2$ i $x=-3$. Rozbij na trzy przedziały i w każdym usuń wartości bezwzględne."
  - "② Dla $x\ge 2$: $(x-2)-2(x+3)<-2\Rightarrow -x-8<-2\Rightarrow x>-6$ — zawsze, więc wszystkie $x\ge 2$. Dla $-3\le x<2$: $-(x-2)-2(x+3)<-2\Rightarrow -3x-4<-2\Rightarrow x>-\tfrac{2}{3}$, więc $x\in(-\tfrac{2}{3},2)$."
  - "③ Dla $x<-3$: $-(x-2)+2(x+3)<-2\Rightarrow x+8<-2\Rightarrow x<-10$. Suma: $(-\infty,-10)\cup(-\tfrac{2}{3},+\infty)$. Uwaga — dokładne odpowiedzi zależą od węzłów; sprawdź wartość graniczną: zadanie CKE kończy się zbiorem $(-\infty,-10)\cup(-\tfrac{2}{3},+\infty)$. Wybierz opcję odpowiadającą poprawnej postaci."
hintsEn:
  - "① The inequality $|x-2|-2|x+3|<-2$ has two \"breakpoints\": $x=2$ and $x=-3$. Split into three intervals and remove the absolute values in each."
  - "② For $x\ge 2$: $(x-2)-2(x+3)<-2\Rightarrow -x-8<-2\Rightarrow x>-6$ — always true, so all $x\ge 2$ work. For $-3\le x<2$: $-(x-2)-2(x+3)<-2\Rightarrow -3x-4<-2\Rightarrow x>-\tfrac{2}{3}$, so $x\in(-\tfrac{2}{3},2)$."
  - "③ For $x<-3$: $-(x-2)+2(x+3)<-2\Rightarrow x+8<-2\Rightarrow x<-10$. Union: $(-\infty,-10)\cup(-\tfrac{2}{3},+\infty)$. Note — the exact answers depend on the breakpoints; check the boundary value: this CKE problem ends with the set $(-\infty,-10)\cup(-\tfrac{2}{3},+\infty)$. Pick the option matching the correct form."
tests:
  abs_value: 1.0
  linear_ineq: 0.4
---
Rozwiąż nierówność $|x-2|-2|x+3|<-2$.

<!-- en -->
Solve the inequality $|x-2|-2|x+3|<-2$.
