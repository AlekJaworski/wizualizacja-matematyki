---
options:
  - "The components of the vector change; the vector itself does not."
  - "The vector itself rotates by 30°."
  - "Both the vector and its components rotate by 30°."
  - "Nothing changes — coordinates are arbitrary so they're irrelevant."
correct: 0
hint: The vector is a physical object — a displacement, a velocity, an arrow in space. Components are the *labels* you put on it after picking a coordinate grid. Rotating the grid relabels; it doesn't move the underlying object.
tests:
  coordinates_as_labels: 1.0
---
You rotate your coordinate grid by 30°. What happens to a vector that was previously labeled $(3, 4)$?
